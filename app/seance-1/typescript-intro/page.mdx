# Introduction à TypeScript

**TypeScript = JavaScript + Types**

TypeScript est un sur-ensemble de JavaScript qui ajoute des types statiques. Tout code JavaScript valide est du TypeScript valide.

<Callout type="info" title="Pourquoi TypeScript ?">
- **Détection d'erreurs** : Les erreurs sont détectées avant l'exécution
- **Autocomplétion** : Votre éditeur vous aide mieux
- **Documentation** : Les types servent de documentation
- **Refactoring** : Plus facile de modifier du code
- **Utilisé partout** : Standard dans l'industrie (React, Next.js, etc.)
</Callout>

## JavaScript vs TypeScript

### Exemple JavaScript (sans types)

```javascript
function greet(name) {
  return `Hello ${name}!`;
}

greet('Alice');     // OK
greet(42);          // OK mais bizarre
greet();            // OK mais name sera undefined
```

**Problème** : JavaScript accepte tout sans se plaindre, les erreurs arrivent à l'exécution.

### Même exemple en TypeScript

```typescript
function greet(name: string): string {
  return `Hello ${name}!`;
}

greet('Alice');     // OK
greet(42);          // Erreur: number n'est pas string
greet();            // Erreur: argument manquant
```

**Avantage** : TypeScript détecte les erreurs **avant** d'exécuter le code.

## Types de base

### Variables typées

```typescript
// Type explicite
const name: string = 'Alice';
const age: number = 25;
const isStudent: boolean = true;

// Type inféré (TypeScript devine le type)
const name = 'Alice';        // TypeScript sait que c'est un string
const age = 25;              // TypeScript sait que c'est un number
const isStudent = true;      // TypeScript sait que c'est un boolean
```

<Callout type="tip">
**Type inference** : TypeScript devine souvent les types tout seul. Pas besoin de tout annoter !
</Callout>

<QuickCheck
  question="Quelle est la différence entre const name: string = 'Alice' et const name = 'Alice' ?"
  options={[
    "La première est invalide en TypeScript",
    "Aucune différence, TypeScript infère le type dans les deux cas",
    "La seconde crée une variable non typée",
    "La première est obligatoire en TypeScript"
  ]}
  correctAnswer={1}
  explanation="TypeScript utilise l'inférence de type : quand vous assignez 'Alice', TypeScript sait automatiquement que c'est un string. L'annotation explicite (: string) est optionnelle mais peut améliorer la lisibilité."
/>

### Tableaux

```typescript
// Tableau de nombres
const numbers: number[] = [1, 2, 3, 4, 5];

// Tableau de strings
const names: string[] = ['Alice', 'Bob', 'Charlie'];

// Syntaxe alternative (même chose)
const numbers: Array<number> = [1, 2, 3, 4, 5];
```

### Objets

```typescript
// Définir la structure d'un objet
const user: { name: string; age: number } = {
  name: 'Alice',
  age: 25
};

// Ou avec une interface (plus lisible)
interface User {
  name: string;
  age: number;
  email?: string;  // ? signifie optionnel
}

const user: User = {
  name: 'Alice',
  age: 25
  // email est optionnel, pas obligé de le mettre
};
```

<QuickCheck
  question="Que signifie le ? dans email?: string ?"
  options={[
    "email est une propriété obligatoire",
    "email est une propriété optionnelle qui peut être absente",
    "email doit être un string vide",
    "email peut être n'importe quel type"
  ]}
  correctAnswer={1}
  explanation="Le ? rend une propriété optionnelle. email?: string signifie que l'objet peut avoir ou non cette propriété, mais si elle existe, elle doit être un string."
/>

## Fonctions

### Typer les paramètres et le retour

```typescript
// Sans types (JavaScript)
function add(a, b) {
  return a + b;
}

// Avec types (TypeScript)
function add(a: number, b: number): number {
  return a + b;
}

add(2, 3);        // OK: 5
add('2', '3');    // Erreur: string n'est pas number
```

### Arrow functions

```typescript
// Fonction fléchée typée
const multiply = (a: number, b: number): number => {
  return a * b;
};

// Avec return implicite
const multiply = (a: number, b: number): number => a * b;

// TypeScript peut inférer le type de retour
const multiply = (a: number, b: number) => a * b;  // retour: number (inféré)
```

### Paramètres optionnels

```typescript
function greet(name: string, greeting?: string): string {
  if (greeting) {
    return `${greeting}, ${name}!`;
  }
  return `Hello, ${name}!`;
}

greet('Alice');              // "Hello, Alice!"
greet('Bob', 'Bonjour');     // "Bonjour, Bob!"
```

### Valeurs par défaut

```typescript
function greet(name: string, greeting: string = 'Hello'): string {
  return `${greeting}, ${name}!`;
}

greet('Alice');              // "Hello, Alice!"
greet('Bob', 'Hi');          // "Hi, Bob!"
```

## Types spéciaux

### any (éviter !)

```typescript
let value: any = 'hello';
value = 42;           // OK
value = true;         // OK
value = [1, 2, 3];    // OK
```

<Callout type="warning">
**Évitez `any` !** Il désactive complètement TypeScript. Utilisez-le seulement si vraiment nécessaire.
</Callout>

### Union types

```typescript
// Variable qui peut être string OU number
let id: string | number;

id = '123';     // OK
id = 123;       // OK
id = true;      // Erreur

// Utile pour les fonctions
function printId(id: string | number) {
  console.log(`ID: ${id}`);
}

printId('ABC123');  // OK
printId(456);       // OK
```

<QuickCheck
  question="Que fait le type string | number ?"
  options={[
    "Convertit un string en number",
    "Permet à une variable d'être soit un string, soit un number",
    "Crée un nouveau type qui est à la fois string et number",
    "Concatène un string et un number"
  ]}
  correctAnswer={1}
  explanation="Le pipe | crée un union type : la variable peut être d'un type OU d'un autre. string | number signifie 'soit string, soit number, mais pas les deux en même temps'."
/>

### Type literal

```typescript
// Variable qui ne peut avoir que certaines valeurs
let status: 'pending' | 'success' | 'error';

status = 'pending';   // OK
status = 'success';   // OK
status = 'loading';   // Erreur: pas dans les valeurs autorisées
```

## Interfaces vs Types

Deux façons de définir des structures :

### Interface

```typescript
interface User {
  name: string;
  age: number;
  email?: string;
}

const user: User = {
  name: 'Alice',
  age: 25
};
```

### Type Alias

```typescript
type User = {
  name: string;
  age: number;
  email?: string;
};

const user: User = {
  name: 'Alice',
  age: 25
};
```

<Callout type="info">
**Interface vs Type** : Pour les objets simples, les deux fonctionnent. Les interfaces sont plus extensibles, les types plus flexibles. Dans la plupart des cas, utilisez ce que vous préférez !
</Callout>

<QuickCheck
  question="Quelle est la principale différence entre interface et type pour définir un objet ?"
  options={[
    "interface est plus rapide à l'exécution",
    "Pour les objets simples, les deux fonctionnent de manière similaire",
    "type ne peut pas définir d'objets",
    "interface est obligatoire avec React"
  ]}
  correctAnswer={1}
  explanation="Pour définir la structure d'un objet, interface et type fonctionnent de manière très similaire. Les deux sont valides et le choix dépend souvent de la préférence personnelle ou des conventions du projet."
/>

## Exemples pratiques

### Tableau d'objets typé

```typescript
interface Student {
  id: number;
  name: string;
  grade: number;
}

const students: Student[] = [
  { id: 1, name: 'Alice', grade: 15 },
  { id: 2, name: 'Bob', grade: 12 },
  { id: 3, name: 'Charlie', grade: 18 }
];

// TypeScript aide avec l'autocomplétion !
const names = students.map(s => s.name);  // TypeScript sait que s est un Student
const passed = students.filter(s => s.grade >= 12);
```

### Fonctions avec objets typés

```typescript
interface Product {
  id: number;
  name: string;
  price: number;
}

function calculateTotal(products: Product[]): number {
  return products.reduce((sum, p) => sum + p.price, 0);
}

const cart: Product[] = [
  { id: 1, name: 'Laptop', price: 1000 },
  { id: 2, name: 'Mouse', price: 25 }
];

const total = calculateTotal(cart);  // 1025
```

## TypeScript avec React (aperçu)

Vous verrez TypeScript en action dans la Séance 2 avec React :

```typescript
// Props typées
interface ButtonProps {
  text: string;
  onClick: () => void;
  disabled?: boolean;
}

function Button({ text, onClick, disabled = false }: ButtonProps) {
  return (
    <button onClick={onClick} disabled={disabled}>
      {text}
    </button>
  );
}

// Utilisation avec autocomplétion complète !
<Button text="Click me" onClick={() => alert('Clicked!')} />
```

## Compiler TypeScript

TypeScript ne s'exécute pas directement dans le navigateur. Il doit être **compilé** en JavaScript.

```bash
# Installer TypeScript
npm install -g typescript

# Compiler un fichier .ts en .js
tsc mon-fichier.ts

# Résultat: mon-fichier.js (JavaScript standard)
```

<Callout type="success">
**Avec React/Next.js** : La compilation est automatique ! Vous écrivez en TypeScript, le framework s'occupe du reste.
</Callout>

## Configuration TypeScript

Un fichier `tsconfig.json` configure TypeScript :

```json
{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "strict": true,
    "esModuleInterop": true
  }
}
```

**Avec Next.js/React** : Ce fichier est généré automatiquement, vous n'avez rien à faire !

## Pourquoi l'apprendre maintenant ?

<div className="my-6 grid grid-cols-1 md:grid-cols-2 gap-4">
  <div className="p-4 bg-green-900/20 border border-green-800 rounded-lg">
    <h3 className="font-semibold text-green-100 mb-2">Avantages</h3>
    <ul className="text-sm text-gray-300 space-y-1">
      <li>Erreurs détectées immédiatement</li>
      <li>Autocomplétion puissante</li>
      <li>Code plus robuste</li>
      <li>Refactoring facile</li>
      <li>Standard de l'industrie</li>
    </ul>
  </div>

  <div className="p-4 bg-blue-900/20 border border-blue-800 rounded-lg">
    <h3 className="font-semibold text-blue-100 mb-2">En pratique</h3>
    <ul className="text-sm text-gray-300 space-y-1">
      <li>React utilise TypeScript</li>
      <li>Next.js supporte TypeScript</li>
      <li>VSCode excellent support</li>
      <li>Documentation vivante</li>
      <li>Meilleure collaboration</li>
    </ul>
  </div>
</div>

<Callout type="tip" title="Comprendre, pas mémoriser">
**Ce qu'il faut retenir :**

- **TypeScript = JavaScript + Types** : tout code JS valide est du TS valide
- **Types de base** : `string`, `number`, `boolean`, `array[]`
- **Interfaces** : définir la structure des objets (`interface User { name: string }`)
- **Type inference** : TypeScript devine souvent les types automatiquement
- **Optional** : `?` rend une propriété optionnelle (`email?: string`)
- **Union types** : `string | number` pour plusieurs types possibles
- **Avoid `any`** : désactive complètement TypeScript

Vous n'avez pas besoin de tout mémoriser maintenant. En Séance 2, vous verrez TypeScript en action avec React et tout deviendra plus concret !
</Callout>

<Exercise
  title="Pratique : TypeScript"
  difficulty="easy"
  solution={
<CodeBlock language="typescript">{`// 1. Interface Book
interface Book {
  title: string;
  author: string;
  year: number;
  pages: number;
  isbn?: string;  // optionnel
}

// 2. Tableau typé
const books: Book[] = [
  { title: '1984', author: 'George Orwell', year: 1949, pages: 328 },
  { title: 'Le Petit Prince', author: 'Antoine de Saint-Exupéry', year: 1943, pages: 96, isbn: '978-2-07-061275-8' }
];

// 3. Fonction avec union type
function findBook(identifier: string | number): Book | undefined {
  if (typeof identifier === 'string') {
    return books.find(b => b.title === identifier);
  } else {
    return books.find(b => b.year === identifier);
  }
}

console.log(findBook('1984'));
// { title: '1984', author: 'George Orwell', year: 1949, pages: 328 }

console.log(findBook(1943));
// { title: 'Le Petit Prince', author: '...', year: 1943, pages: 96, isbn: '...' }

// 4. Fonction calculateAveragePages
function calculateAveragePages(books: Book[]): number {
  const total = books.reduce((sum, book) => sum + book.pages, 0);
  return total / books.length;
}

console.log(calculateAveragePages(books)); // 212`}
</CodeBlock>
  }
>
Créez une petite bibliothèque avec TypeScript :

1. Créer une interface `Book` avec :
   - `title`: string
   - `author`: string
   - `year`: number
   - `pages`: number
   - `isbn`: string (optionnel)

2. Créer un tableau `books: Book[]` avec au moins 2 livres

3. Créer une fonction `findBook(identifier: string | number)` qui :
   - Cherche un livre par titre si `identifier` est un string
   - Cherche un livre par année si `identifier` est un number
   - Retourne `Book | undefined`

4. Créer une fonction `calculateAveragePages(books: Book[]): number` qui calcule la moyenne de pages

**Astuce :** Utilisez `typeof` pour vérifier le type à l'exécution dans les union types.
</Exercise>

## Prochaine étape

<Callout type="tip">
**Ne vous inquiétez pas !**

Vous n'avez pas besoin de mémoriser toute la syntaxe TypeScript maintenant. En Séance 2, nous l'utiliserons naturellement avec React, et vous verrez à quel point c'est utile dans un vrai projet.

L'essentiel : comprendre que TypeScript = JavaScript + types pour vous aider.
</Callout>

<Resources resources={[
  {
    title: "TypeScript Handbook",
    description: "Documentation officielle TypeScript",
    url: "https://www.typescriptlang.org/docs/handbook/intro.html",
    type: "doc"
  },
  {
    title: "TypeScript for JavaScript Programmers",
    description: "Guide rapide pour passer de JS à TS",
    url: "https://www.typescriptlang.org/docs/handbook/typescript-in-5-minutes.html",
    type: "tutorial"
  },
  {
    title: "TypeScript Playground",
    description: "Testez TypeScript en ligne",
    url: "https://www.typescriptlang.org/play",
    type: "tutorial"
  }
]} />
