# Tableaux (Arrays)

Les tableaux sont des collections ordonnées d'éléments, essentielles en JavaScript pour manipuler des listes de données.

## Création et accès

```javascript
const fruits = ['pomme', 'banane', 'orange'];

// Accès par index (commence à 0)
console.log(fruits[0]); // 'pomme'
console.log(fruits[1]); // 'banane'

// Taille du tableau
console.log(fruits.length); // 3
```

<Callout type="info">
Contrairement à des langages comme C ou Java, les tableaux JavaScript peuvent contenir **plusieurs types différents** :

```javascript
const mixed = [1, 'hello', true, { name: 'Alice' }]; // ✅ Valide
```

**Cependant, cette pratique n'est pas recommandée.** Privilégiez des tableaux homogènes (un seul type) pour un code plus prévisible et plus facile à maintenir.
</Callout>

## Méthodes de base

```javascript
const numbers = [1, 2, 3];

// Ajouter à la fin
numbers.push(4); // [1, 2, 3, 4]

// Retirer le dernier
numbers.pop(); // [1, 2, 3]

// Vérifier la présence
numbers.includes(2); // true

// Trouver l'index
numbers.indexOf(2); // 1

// Vérifier si c'est un tableau
Array.isArray(numbers); // true
```

<QuickCheck
  question="Que retourne fruits[fruits.length - 1] si fruits = ['pomme', 'banane', 'orange'] ?"
  options={[
    "'pomme'",
    "'orange'",
    "undefined",
    "3"
  ]}
  correctAnswer={1}
  explanation="fruits.length est 3, donc fruits[3 - 1] = fruits[2] = 'orange'. C'est une astuce courante pour accéder au dernier élément d'un tableau."
/>

## Méthodes de transformation

Ces méthodes sont **essentielles** en JavaScript car elles créent de nouveaux tableaux sans modifier l'original (immutabilité).

<Callout type="info" title="Pourquoi l'immutabilité ?">
Les méthodes comme `.map()`, `.filter()`, `.reduce()` ne modifient jamais le tableau original. Elles en créent un nouveau.

**Avantages :**
- Code plus prévisible et facile à déboguer
- Évite les effets de bord (side effects)
- Facilite le suivi des changements de données
- Bonne pratique pour les applications web

**Privilégiez toujours les méthodes immutables**.
</Callout>

### Les méthodes essentielles

```javascript
const numbers = [1, 2, 3, 4, 5];
const users = [
  { id: 1, name: 'Alice', age: 25 },
  { id: 2, name: 'Bob', age: 30 }
];

// .map() - Transforme chaque élément
const doubled = numbers.map(n => n * 2);
// [2, 4, 6, 8, 10]

const names = users.map(user => user.name);
// ['Alice', 'Bob']

// .filter() - Garde les éléments qui passent le test
const evens = numbers.filter(n => n % 2 === 0);
// [2, 4]

const adults = users.filter(user => user.age >= 18);
// [{ id: 1, name: 'Alice', age: 25 }, { id: 2, name: 'Bob', age: 30 }]

// .find() - Retourne le premier élément qui correspond
const bob = users.find(user => user.id === 2);
// { id: 2, name: 'Bob', age: 30 }

// .some() - Au moins un élément passe le test ?
numbers.some(n => n > 3); // true

// .every() - Tous les éléments passent le test ?
numbers.every(n => n > 0); // true

// .reduce() - Réduit à une seule valeur
const sum = numbers.reduce((acc, n) => acc + n, 0);
// 15 (1+2+3+4+5)
```

<QuickCheck
  question="Que retourne [1, 2, 3].map(n => n * n) ?"
  options={[
    "[1, 2, 3]",
    "[2, 4, 6]",
    "[1, 4, 9]",
    "[3, 6, 9]"
  ]}
  correctAnswer={2}
  explanation=".map(n => n * n) multiplie chaque nombre par lui-même (carré), donc 1*1=1, 2*2=4, 3*3=9. Le résultat est [1, 4, 9]."
/>

<QuickCheck
  question="Quelle différence entre .map() et .filter() ?"
  options={[
    ".map() retourne toujours le même nombre d'éléments, .filter() peut en retourner moins",
    ".map() retourne un boolean, .filter() retourne un tableau",
    ".filter() transforme les éléments, .map() les supprime",
    "Aucune différence, ce sont des alias"
  ]}
  correctAnswer={0}
  explanation=".map() transforme chaque élément et retourne un tableau de même taille. .filter() garde uniquement les éléments qui passent le test, donc peut retourner un tableau plus petit."
/>

<QuickCheck
  question="Que fait le deuxième paramètre de .reduce() ?"
  options={[
    "Il définit le nombre d'itérations",
    "Il définit la valeur initiale de l'accumulateur",
    "Il filtre les éléments avant de les réduire",
    "Il définit le type de retour"
  ]}
  correctAnswer={1}
  explanation="Le deuxième paramètre (0 dans reduce((acc, n) => acc + n, 0)) est la valeur initiale de l'accumulateur. Sans cette valeur, reduce() utilise le premier élément du tableau comme valeur initiale."
/>

<Callout type="tip" title="Comprendre, pas mémoriser">
**Vous n'avez pas besoin de mémoriser toutes ces méthodes !**

L'important est de comprendre les concepts :
- `.map()` → transformer
- `.filter()` → filtrer
- `.find()` → chercher
- `.reduce()` → accumuler

Avec la pratique, ces méthodes deviendront naturelles. Consultez la documentation (MDN) quand vous avez besoin d'un rappel sur la syntaxe.
</Callout>

## Opérations immutables

Pour copier ou modifier des tableaux sans mutation, utilisez ces techniques :

```javascript
const original = [1, 2, 3];

// Copier un tableau
const copy1 = original.slice(); // Méthode classique
const copy2 = [...original]; // Spread operator

// Ajouter des éléments (sans push)
const withFour = [...original, 4]; // [1, 2, 3, 4]
const withZero = [0, ...original]; // [0, 1, 2, 3]

// Combiner des tableaux (sans mutation)
const arr1 = [1, 2];
const arr2 = [3, 4];
const combined = [...arr1, ...arr2]; // [1, 2, 3, 4]
// Ou : arr1.concat(arr2)

// Retirer un élément par index
const numbers = [1, 2, 3, 4, 5];
const index = 2;
const withoutIndex = [
  ...numbers.slice(0, index),
  ...numbers.slice(index + 1)
];
console.log(withoutIndex); // [1, 2, 4, 5]
```

<QuickCheck
  question="Quelle est la différence entre [1, 2, 3].push(4) et [...[1, 2, 3], 4] ?"
  options={[
    "Aucune différence",
    ".push() modifie le tableau original, [...array, 4] crée un nouveau tableau",
    ".push() crée un nouveau tableau, [...array, 4] modifie l'original",
    ".push() est plus moderne que le spread operator"
  ]}
  correctAnswer={1}
  explanation=".push() est une méthode mutante qui modifie le tableau original et retourne la nouvelle longueur. Le spread operator [...array, 4] crée un NOUVEAU tableau sans toucher à l'original. Privilégiez les opérations immutables."
/>

<Exercise
  title="Pratique : Méthodes de tableaux"
  difficulty="easy"
  solution={
    <CodeBlock language="javascript">{`const products = [
  { name: 'Laptop', price: 1000 },
  { name: 'Mouse', price: 25 },
  { name: 'Keyboard', price: 75 }
];

// 1. Tous les noms
const names = products.map(p => p.name);
// ['Laptop', 'Mouse', 'Keyboard']

// 2. Produits < 100€
const cheap = products.filter(p => p.price < 100);
// [{ name: 'Mouse', ... }, { name: 'Keyboard', ... }]

// 3. Prix total
const total = products.reduce((sum, p) => sum + p.price, 0);
// 1100`}</CodeBlock>
  }
>
Avec ce tableau de produits :

<CodeBlock language="javascript">{`const products = [
  { name: 'Laptop', price: 1000 },
  { name: 'Mouse', price: 25 },
  { name: 'Keyboard', price: 75 }
];`}</CodeBlock>

1. Extraire tous les noms dans un tableau
2. Filtrer les produits à moins de 100€
3. Calculer le prix total

**Astuce :** Utilisez `.map()`, `.filter()` et `.reduce()`
</Exercise>


## Tableau de syntaxe

<table className="w-full">
  <thead>
    <tr className="border-b-2 border-gray-300 dark:border-gray-600">
      <th className="text-left">Méthode</th>
      <th className="text-left">Usage</th>
      <th className="text-left">Retourne</th>
      <th className="text-left">Mutable ?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>.map()</code></td>
      <td>Transformer chaque élément</td>
      <td>Nouveau tableau</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.filter()</code></td>
      <td>Filtrer des éléments</td>
      <td>Nouveau tableau</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.find()</code></td>
      <td>Trouver un élément</td>
      <td>Élément ou undefined</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.some()</code></td>
      <td>Tester au moins un</td>
      <td>boolean</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.every()</code></td>
      <td>Tester tous</td>
      <td>boolean</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.reduce()</code></td>
      <td>Accumuler/réduire</td>
      <td>Une valeur</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.slice()</code></td>
      <td>Copier/extraire</td>
      <td>Nouveau tableau</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>[...array]</code></td>
      <td>Copier (spread)</td>
      <td>Nouveau tableau</td>
      <td>❌ Non</td>
    </tr>
    <tr>
      <td><code>.push()</code></td>
      <td>Ajouter à la fin</td>
      <td>Nouvelle longueur</td>
      <td>✅ Oui</td>
    </tr>
    <tr>
      <td><code>.pop()</code></td>
      <td>Retirer le dernier</td>
      <td>Élément retiré</td>
      <td>✅ Oui</td>
    </tr>
  </tbody>
</table>

<Callout type="success" title="Bonne pratique">
**Privilégiez toujours les méthodes immutables** (`.map()`, `.filter()`, `.slice()`, spread operator) plutôt que les méthodes mutantes (`.push()`, `.pop()`, `.splice()`).

Votre code sera plus prévisible, plus facile à déboguer, et maintenable.
</Callout>

