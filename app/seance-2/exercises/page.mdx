# Exercices - React Fondamentaux

Mettez en pratique ce que vous avez appris : composants, props, listes et rendu conditionnel !

<Callout type="tip" title="Comment travailler efficacement">
- **Créez un projet Vite** : `npm create vite@latest react-exercises -- --template react-ts`
- **Testez régulièrement** : Lancez `npm run dev` et vérifiez dans le navigateur
- **Utilisez React DevTools** : Installez l'extension pour déboguer
- **Essayez d'abord** : Tentez de résoudre avant de regarder la solution
- **Comprenez le code** : L'objectif est de comprendre, pas de copier !

💡 **Temps estimé** : 1h30 pour les 4 exercices
</Callout>

## Exercice 1 : Composant avec props

<Exercise
  title="Carte de profil utilisateur"
  difficulty="easy"
  solution={
    <CodeBlock language="tsx">{`// ProfileCard.tsx
interface ProfileCardProps {
  name: string;
  bio: string;
  avatarUrl: string;
  isOnline?: boolean;
}

function ProfileCard({ name, bio, avatarUrl, isOnline = false }: ProfileCardProps) {
  return (
    <div className="profile-card">
      <img src={avatarUrl} alt={name} width={80} height={80} />
      <h3>{name}</h3>
      <p>{bio}</p>
      {isOnline && <span className="badge">🟢 En ligne</span>}
    </div>
  );
}

export default ProfileCard;

// App.tsx
import ProfileCard from './ProfileCard';

function App() {
  return (
    <div>
      <h1>Équipe</h1>
      <ProfileCard
        name="Alice Dupont"
        bio="Développeuse frontend passionnée par React"
        avatarUrl="https://i.pravatar.cc/150?img=1"
        isOnline={true}
      />
      <ProfileCard
        name="Bob Martin"
        bio="Designer UI/UX et expert en accessibilité"
        avatarUrl="https://i.pravatar.cc/150?img=3"
        isOnline={false}
      />
      <ProfileCard
        name="Claire Rousseau"
        bio="Chef de projet et Scrum Master certifiée"
        avatarUrl="https://i.pravatar.cc/150?img=5"
        isOnline={true}
      />
    </div>
  );
}`}</CodeBlock>
  }
>
Créez un composant `ProfileCard` qui affiche une carte de profil utilisateur.

**Props attendues (avec TypeScript) :**
- `name` : string (requis)
- `bio` : string (requis)
- `avatarUrl` : string (requis)
- `isOnline` : boolean (optionnel, par défaut false)

**Affichage :**
- Image de profil (80x80px)
- Nom en titre
- Biographie
- Badge "🟢 En ligne" uniquement si `isOnline` est true

**Utilisation :**

<CodeBlock language="tsx">{`<ProfileCard
  name="Alice Dupont"
  bio="Développeuse frontend"
  avatarUrl="https://i.pravatar.cc/150?img=1"
  isOnline={true}
/>`}</CodeBlock>

**Dans App.tsx :** Créez 3 cartes de profil différentes

<Callout type="tip" title="Rappel">
Utilisez l'opérateur `&&` pour afficher conditionnellement le badge :

```tsx
{isOnline && <span>🟢 En ligne</span>}
```
</Callout>
</Exercise>

## Exercice 2 : Listes et keys

<Exercise
  title="Liste de produits avec filtrage"
  difficulty="medium"
  solution={
    <CodeBlock language="tsx">{`// types.ts
export interface Product {
  id: number;
  name: string;
  price: number;
  category: 'electronics' | 'clothing' | 'books';
}

// ProductCard.tsx
import { Product } from './types';

interface ProductCardProps {
  product: Product;
}

function ProductCard({ product }: ProductCardProps) {
  return (
    <div className="product-card">
      <h3>{product.name}</h3>
      <p className="price">{product.price}€</p>
      <span className="category">{product.category}</span>
    </div>
  );
}

export default ProductCard;

// ProductList.tsx
import { Product } from './types';
import ProductCard from './ProductCard';

interface ProductListProps {
  products: Product[];
  category?: string;
}

function ProductList({ products, category }: ProductListProps) {
  // Filtrer par catégorie si fournie
  const filteredProducts = category
    ? products.filter(p => p.category === category)
    : products;

  // Afficher un message si vide
  if (filteredProducts.length === 0) {
    return <p className="empty">Aucun produit trouvé</p>;
  }

  return (
    <div className="product-list">
      {filteredProducts.map(product => (
        <ProductCard key={product.id} product={product} />
      ))}
    </div>
  );
}

export default ProductList;

// App.tsx
import { Product } from './types';
import ProductList from './ProductList';

const products: Product[] = [
  { id: 1, name: 'Laptop', price: 999, category: 'electronics' },
  { id: 2, name: 'T-shirt', price: 29, category: 'clothing' },
  { id: 3, name: 'Smartphone', price: 699, category: 'electronics' },
  { id: 4, name: 'Roman', price: 15, category: 'books' },
  { id: 5, name: 'Casque audio', price: 149, category: 'electronics' }
];

function App() {
  return (
    <div>
      <h1>Catalogue</h1>

      <h2>Tous les produits</h2>
      <ProductList products={products} />

      <h2>Électronique</h2>
      <ProductList products={products} category="electronics" />

      <h2>Vêtements</h2>
      <ProductList products={products} category="clothing" />
    </div>
  );
}`}</CodeBlock>
  }
>
Créez un système d'affichage de produits avec filtrage par catégorie.

**Données de test :**

<CodeBlock language="tsx">{`interface Product {
  id: number;
  name: string;
  price: number;
  category: 'electronics' | 'clothing' | 'books';
}

const products: Product[] = [
  { id: 1, name: 'Laptop', price: 999, category: 'electronics' },
  { id: 2, name: 'T-shirt', price: 29, category: 'clothing' },
  { id: 3, name: 'Smartphone', price: 699, category: 'electronics' },
  { id: 4, name: 'Roman', price: 15, category: 'books' },
  { id: 5, name: 'Casque audio', price: 149, category: 'electronics' }
];`}</CodeBlock>

**Composants à créer :**

**1. `ProductCard`**
- Props : `product` (objet Product)
- Affiche : nom, prix, catégorie

**2. `ProductList`**
- Props : `products` (tableau), `category` (string optionnel)
- Filtre par catégorie si fournie
- Affiche "Aucun produit trouvé" si la liste filtrée est vide
- Utilise `.map()` pour afficher les ProductCard
- **N'oubliez pas les keys !**

**Utilisation :**

<CodeBlock language="tsx">{`// Tous les produits
<ProductList products={products} />

// Uniquement l'électronique
<ProductList products={products} category="electronics" />

// Uniquement les vêtements
<ProductList products={products} category="clothing" />`}</CodeBlock>

**Points importants :**
- ✅ Chaque `ProductCard` doit avoir une `key` unique (utilisez `product.id`)
- ✅ Le filtrage doit fonctionner correctement
- ✅ Gérer le cas de liste vide

<Callout type="tip" title="Astuce pour le filtrage">
Utilisez l'opérateur ternaire pour filtrer conditionnellement :

```tsx
const filteredProducts = category
  ? products.filter(p => p.category === category)
  : products;
```

Puis utilisez `.map()` sur `filteredProducts`
</Callout>
</Exercise>

## Exercice 3 : Rendu conditionnel

<Exercise
  title="Dashboard avec états de chargement"
  difficulty="medium"
  solution={
    <CodeBlock language="tsx">{`// types.ts
export interface Stat {
  id: number;
  title: string;
  value: number;
  trend: string;
}

// StatCard.tsx
import { Stat } from './types';

interface StatCardProps {
  stat: Stat;
}

function StatCard({ stat }: StatCardProps) {
  const isPositive = stat.trend.startsWith('+');

  return (
    <div className="stat-card">
      <h3>{stat.title}</h3>
      <p className="value">{stat.value}</p>
      <span className={isPositive ? 'trend-positive' : 'trend-negative'}>
        {stat.trend}
      </span>
    </div>
  );
}

export default StatCard;

// Dashboard.tsx
import { Stat } from './types';
import StatCard from './StatCard';

interface DashboardProps {
  loading: boolean;
  error: string | null;
  data: Stat[] | null;
}

function Dashboard({ loading, error, data }: DashboardProps) {
  // État 1 : Loading
  if (loading) {
    return (
      <div className="dashboard-loading">
        <p>⏳ Chargement des données...</p>
      </div>
    );
  }

  // État 2 : Error
  if (error) {
    return (
      <div className="dashboard-error">
        <p>❌ Erreur : {error}</p>
        <button onClick={() => window.location.reload()}>
          Réessayer
        </button>
      </div>
    );
  }

  // État 3 : Empty
  if (!data || data.length === 0) {
    return (
      <div className="dashboard-empty">
        <p>📭 Aucune donnée disponible</p>
      </div>
    );
  }

  // État 4 : Success
  return (
    <div className="dashboard">
      <h1>Tableau de bord</h1>
      <div className="stats-grid">
        {data.map(stat => (
          <StatCard key={stat.id} stat={stat} />
        ))}
      </div>
    </div>
  );
}

export default Dashboard;

// App.tsx
import { Stat } from './types';
import Dashboard from './Dashboard';

const mockData: Stat[] = [
  { id: 1, title: 'Ventes', value: 1234, trend: '+12%' },
  { id: 2, title: 'Utilisateurs', value: 567, trend: '+8%' },
  { id: 3, title: 'Revenus', value: 8910, trend: '-3%' }
];

function App() {
  // Testez les 4 états différents (décommentez un seul à la fois)

  // État 1 : Loading
  // return <Dashboard loading={true} error={null} data={null} />;

  // État 2 : Error
  // return <Dashboard loading={false} error="Connexion au serveur impossible" data={null} />;

  // État 3 : Empty
  // return <Dashboard loading={false} error={null} data={[]} />;

  // État 4 : Success
  return <Dashboard loading={false} error={null} data={mockData} />;
}`}</CodeBlock>
  }
>
Créez un dashboard qui gère 4 états différents : loading, error, empty, et success.

**Données de test :**

<CodeBlock language="tsx">{`interface Stat {
  id: number;
  title: string;
  value: number;
  trend: string;
}

const mockData: Stat[] = [
  { id: 1, title: 'Ventes', value: 1234, trend: '+12%' },
  { id: 2, title: 'Utilisateurs', value: 567, trend: '+8%' },
  { id: 3, title: 'Revenus', value: 8910, trend: '-3%' }
];`}</CodeBlock>

**Composants à créer :**

**1. `StatCard`**
- Props : `stat` (objet Stat)
- Affiche : titre, valeur, tendance
- Colore la tendance en vert si positive (+), rouge si négative (-)

**2. `Dashboard`**
- Props : `loading` (boolean), `error` (string | null), `data` (Stat[] | null)
- Gère 4 états avec des **early returns** :

**États à gérer :**
1. **Loading** (`loading === true`) → Affiche "⏳ Chargement des données..."
2. **Error** (`error !== null`) → Affiche "❌ Erreur : [message]" + bouton "Réessayer"
3. **Empty** (`data === null` ou `data.length === 0`) → Affiche "📭 Aucune donnée disponible"
4. **Success** (sinon) → Affiche le titre + grille de StatCard

**Tester les 4 états :**

<CodeBlock language="tsx">{`// État 1 : Loading
<Dashboard loading={true} error={null} data={null} />

// État 2 : Error
<Dashboard loading={false} error="Connexion impossible" data={null} />

// État 3 : Empty
<Dashboard loading={false} error={null} data={[]} />

// État 4 : Success
<Dashboard loading={false} error={null} data={mockData} />`}</CodeBlock>

<Callout type="tip" title="Pattern des early returns">
Utilisez des `if` avec `return` pour simplifier la logique :

```tsx
function Dashboard({ loading, error, data }) {
  if (loading) return <LoadingView />;
  if (error) return <ErrorView error={error} />;
  if (!data || data.length === 0) return <EmptyView />;

  // État success
  return <SuccessView data={data} />;
}
```

Ce pattern évite les ternaires imbriqués et rend le code plus lisible !
</Callout>
</Exercise>

## Exercice 4 : Lecture et validation de code

<Callout type="warning" title="Compétence essentielle">
Savoir **lire et valider** du code React est crucial quand vous travaillez avec l'IA. Vous devez pouvoir **détecter les erreurs**, **comprendre les bugs**, et **corriger les mauvaises pratiques**.
</Callout>

<Exercise
  title="Trouver les bugs dans le code React"
  difficulty="medium"
  solution={
    <div className="space-y-4">
      <div>
        <p className="font-semibold mb-2">Code 1 - Problème : Keys manquantes</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : Pas de key prop
const users = ['Alice', 'Bob', 'Charlie'];

return (
  <ul>
    {users.map(user => (
      <li>{user}</li>  // ❌ Warning: Each child should have a unique "key"
    ))}
  </ul>
);

// ✅ CORRECT : Avec key unique
// Si pas d'ID unique, utilisez l'index EN DERNIER RECOURS
return (
  <ul>
    {users.map((user, index) => (
      <li key={index}>{user}</li>  // ✅ Mais mieux avec un vrai ID !
    ))}
  </ul>
);

// ✅ MEILLEUR : Avec ID unique
const users = [
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' },
  { id: 3, name: 'Charlie' }
];

return (
  <ul>
    {users.map(user => (
      <li key={user.id}>{user.name}</li>  // ✅ ID unique stable
    ))}
  </ul>
);`}</CodeBlock>
      </div>

      <div>
        <p className="font-semibold mb-2">Code 2 - Problème : Mauvaise utilisation de .map()</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : .map() avec condition retourne undefined
const users = [
  { name: 'Alice', age: 25 },
  { name: 'Bob', age: 17 },
  { name: 'Charlie', age: 30 }
];

const adults = users.map(user => {
  if (user.age >= 18) return <div>{user.name}</div>;
});
// adults = [<div>Alice</div>, undefined, <div>Charlie</div>]
// React affiche un trou vide ! ❌

// ✅ CORRECT : Filtrer d'abord, puis mapper
const adults = users
  .filter(user => user.age >= 18)
  .map(user => <div key={user.name}>{user.name}</div>);
// adults = [<div>Alice</div>, <div>Charlie</div>] ✅`}</CodeBlock>
      </div>

      <div>
        <p className="font-semibold mb-2">Code 3 - Problème : Utiliser l'index comme key sur liste dynamique</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : Index comme key sur liste qui change
const [tasks, setTasks] = useState([
  { text: 'Apprendre React' },
  { text: 'Faire les exercices' },
  { text: 'Créer un projet' }
]);

// Si on supprime "Faire les exercices", React perd la trace !
return (
  <ul>
    {tasks.map((task, index) => (
      <li key={index}>{task.text}</li>  // ❌ Index change si liste modifiée
    ))}
  </ul>
);

// ✅ CORRECT : ID unique stable
const [tasks, setTasks] = useState([
  { id: 1, text: 'Apprendre React' },
  { id: 2, text: 'Faire les exercices' },
  { id: 3, text: 'Créer un projet' }
]);

return (
  <ul>
    {tasks.map(task => (
      <li key={task.id}>{task.text}</li>  // ✅ ID ne change jamais
    ))}
  </ul>
);`}</CodeBlock>
      </div>

      <div>
        <p className="font-semibold mb-2">Code 4 - Problème : Oublier le return dans .map()</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : Accolades sans return
const numbers = [1, 2, 3, 4, 5];

return (
  <ul>
    {numbers.map(n => {
      <li>{n}</li>  // ❌ Oublié le return ! Affiche rien
    })}
  </ul>
);

// ✅ CORRECT : Avec return explicite
return (
  <ul>
    {numbers.map(n => {
      return <li key={n}>{n}</li>;  // ✅ Return explicite
    })}
  </ul>
);

// ✅ MEILLEUR : Return implicite (sans accolades)
return (
  <ul>
    {numbers.map(n => (
      <li key={n}>{n}</li>  // ✅ Parenthèses = return implicite
    ))}
  </ul>
);`}</CodeBlock>
      </div>

      <div>
        <p className="font-semibold mb-2">Code 5 - Problème : Props mal typées en TypeScript</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : Props non typées
function UserCard(props) {  // ❌ Pas de types
  return (
    <div>
      <h3>{props.name}</h3>
      <p>{props.age}</p>
    </div>
  );
}

// App.tsx
<UserCard name="Alice" age="25" />  // ❌ age est un string au lieu de number

// ✅ CORRECT : Avec interface TypeScript
interface UserCardProps {
  name: string;
  age: number;
}

function UserCard({ name, age }: UserCardProps) {
  return (
    <div>
      <h3>{name}</h3>
      <p>{age} ans</p>
    </div>
  );
}

// App.tsx
<UserCard name="Alice" age={25} />  // ✅ TypeScript vérifie le type !
// <UserCard name="Alice" age="25" />  // ❌ Erreur de compilation détectée`}</CodeBlock>
      </div>

      <div>
        <p className="font-semibold mb-2">Code 6 - Problème : Condition avec 0 et l'opérateur &&</p>
        <CodeBlock language="tsx">{`// ❌ MAUVAIS : Affiche "0" à l'écran si count === 0
const count = 0;

return (
  <div>
    {count && <p>Vous avez {count} message(s)</p>}
  </div>
);
// Affiche : 0  (React affiche le nombre 0 !) ❌

// ✅ CORRECT : Comparaison explicite
return (
  <div>
    {count > 0 && <p>Vous avez {count} message(s)</p>}
  </div>
);
// N'affiche rien si count === 0 ✅

// ✅ ALTERNATIF : Opérateur ternaire
return (
  <div>
    {count > 0 ? <p>Vous avez {count} message(s)</p> : null}
  </div>
);`}</CodeBlock>
      </div>
    </div>
  }
>
Lisez attentivement les 6 codes suivants. **Chacun contient un bug ou une mauvaise pratique React.**

Pour chaque code :
1. **Identifiez le problème**
2. **Expliquez pourquoi c'est un bug**
3. **Proposez la correction**

### Code 1 : Liste sans keys

<CodeBlock language="tsx">{`const users = ['Alice', 'Bob', 'Charlie'];

return (
  <ul>
    {users.map(user => (
      <li>{user}</li>
    ))}
  </ul>
);`}</CodeBlock>

**Questions :**
- Quelle erreur s'affiche dans la console ?
- Pourquoi React a-t-il besoin de keys ?
- Comment corriger ce code ?

---

### Code 2 : Filtrage avec .map()

<CodeBlock language="tsx">{`const users = [
  { name: 'Alice', age: 25 },
  { name: 'Bob', age: 17 },
  { name: 'Charlie', age: 30 }
];

const adults = users.map(user => {
  if (user.age >= 18) return <div>{user.name}</div>;
});

return <div>{adults}</div>;`}</CodeBlock>

**Questions :**
- Que contient exactement le tableau `adults` ?
- Quel est le problème avec cette approche ?
- Quelle méthode devrait être utilisée avant `.map()` ?

---

### Code 3 : Index comme key

<CodeBlock language="tsx">{`const tasks = [
  { text: 'Apprendre React' },
  { text: 'Faire les exercices' },
  { text: 'Créer un projet' }
];

return (
  <ul>
    {tasks.map((task, index) => (
      <li key={index}>{task.text}</li>
    ))}
  </ul>
);`}</CodeBlock>

**Questions :**
- Pourquoi utiliser l'index comme key est problématique ?
- Que se passe-t-il si on supprime le 2ème élément ?
- Quelle est la bonne solution ?

---

### Code 4 : Oubli du return

<CodeBlock language="tsx">{`const numbers = [1, 2, 3, 4, 5];

return (
  <ul>
    {numbers.map(n => {
      <li>{n}</li>
    })}
  </ul>
);`}</CodeBlock>

**Questions :**
- Qu'affiche ce code à l'écran ?
- Quelle est l'erreur ?
- Comment corriger (deux solutions) ?

---

### Code 5 : Props non typées

<CodeBlock language="tsx">{`function UserCard(props) {
  return (
    <div>
      <h3>{props.name}</h3>
      <p>{props.age} ans</p>
    </div>
  );
}

// Utilisation
<UserCard name="Alice" age="25" />`}</CodeBlock>

**Questions :**
- Quel est le problème avec `age="25"` ?
- Comment TypeScript peut-il nous aider ?
- Écrivez l'interface correcte pour les props

---

### Code 6 : Condition avec 0

<CodeBlock language="tsx">{`const count = 0;

return (
  <div>
    <h1>Messages</h1>
    {count && <p>Vous avez {count} message(s)</p>}
  </div>
);`}</CodeBlock>

**Questions :**
- Qu'affiche ce code à l'écran quand `count === 0` ?
- Pourquoi React affiche-t-il `0` ?
- Comment corriger ce problème ?

---

<Callout type="tip" title="Pourquoi cet exercice est important">
Quand vous utilisez l'IA pour générer du code React :
- **L'IA peut faire ces erreurs** (keys manquantes, index, types incorrects)
- **Vous devez pouvoir les détecter** avant d'utiliser le code
- **Comprendre le code** est plus important que le copier-coller
- **TypeScript aide** mais ne détecte pas tout (comme les keys)

Cet exercice vous entraîne à **lire, analyser et valider** du code React généré.
</Callout>
</Exercise>

---

## Récapitulatif

<Callout type="info" title="Ce que vous avez pratiqué">
**Exercice 1 :** Créer des composants avec props typées (TypeScript)

**Exercice 2 :** Afficher des listes avec `.map()`, gérer les keys, filtrer

**Exercice 3 :** Gérer plusieurs états avec rendu conditionnel (early returns)

**Exercice 4 :** Lire et valider du code React, détecter les bugs courants

**Temps total :** ~1h30

**Prochaine étape :** Le projet fil rouge (application d'apprentissage du japonais)
</Callout>

<Resources resources={[
  {
    title: "React Docs - Composants et Props",
    description: "Guide officiel sur les composants et les props",
    url: "https://react.dev/learn/passing-props-to-a-component",
    type: "doc"
  },
  {
    title: "React Docs - Rendu de listes",
    description: "Tout sur .map(), keys et listes React",
    url: "https://react.dev/learn/rendering-lists",
    type: "doc"
  },
  {
    title: "React Docs - Rendu conditionnel",
    description: "Les différentes méthodes de rendu conditionnel",
    url: "https://react.dev/learn/conditional-rendering",
    type: "doc"
  },
  {
    title: "React DevTools",
    description: "Extension Chrome/Firefox pour déboguer React",
    url: "https://react.dev/learn/react-developer-tools",
    type: "tool"
  }
]} />
