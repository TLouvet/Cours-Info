# Props - Passer des données aux composants

Les **props** (propriétés) permettent de passer des données d'un composant parent à un composant enfant. C'est la base de la communication en React !

## Qu'est-ce qu'une prop ?

Une **prop** est comme un **argument** de fonction : elle permet de personnaliser un composant.

```jsx
// Sans props (statique)
function Greeting() {
  return <h1>Bonjour Alice !</h1>;
}

// Avec props (dynamique)
function Greeting(props) {
  return <h1>Bonjour {props.name} !</h1>;
}

// Utilisation
<Greeting name="Alice" />
<Greeting name="Bob" />
```

<Callout type="info" title="Props = Arguments de fonction">
Pensez aux props comme aux **arguments d'une fonction** :

```javascript
// Fonction JavaScript normale
function greet(name) {
  return `Bonjour ${name}`;
}
greet('Alice'); // "Bonjour Alice"

// Composant React avec prop
function Greeting({ name }) {
  return <h1>Bonjour {name}</h1>;
}
<Greeting name="Alice" /> // Affiche "Bonjour Alice"
```
</Callout>

## Passer des props

Vous passez des props comme des **attributs HTML**.

```jsx
function App() {
  return (
    <div>
      <Greeting name="Alice" age={25} />
      <Greeting name="Bob" age={30} />
    </div>
  );
}
```

### Types de props

**Chaînes de caractères :**
```jsx
<UserCard name="Alice" />
```

**Nombres, booléens, objets, tableaux :**
```jsx
<ProductCard price={99} inStock={true} />
<UserCard user={{ name: 'Alice', age: 25 }} />
```

<Callout type="warning" title="Attention aux types">
**Chaînes** : Avec ou sans accolades
```jsx
<UserCard name="Alice" />  // ✅
<UserCard name={'Alice'} /> // ✅ Équivalent
```

**Autres types** : Toujours avec accolades
```jsx
<ProductCard price={99} />        // ✅ Number
<ProductCard price="99" />        // ❌ String "99"

<Toggle enabled={true} />         // ✅ Boolean
<Toggle enabled="true" />         // ❌ String "true"
```
</Callout>

<QuickCheck
  question="Comment passe-t-on un nombre en tant que prop ?"
  options={[
    '<Component count=\"5\" />',
    '<Component count=5 />',
    '<Component count={5} />',
    '<Component count=(5) />'
  ]}
  correctAnswer={2}
  explanation="Les valeurs non-string doivent être passées entre accolades {}. Les nombres, booléens, objets et tableaux nécessitent tous des accolades. Seules les chaînes de caractères peuvent être passées avec des guillemets simples."
/>

## Recevoir des props

### Méthode 1 : Objet `props`

```jsx
function Greeting(props) {
  return (
    <div>
      <h1>Bonjour {props.name} !</h1>
      <p>Tu as {props.age} ans.</p>
    </div>
  );
}
```

### Méthode 2 : Destructuration (recommandé)

```jsx
function Greeting({ name, age }) {
  return (
    <div>
      <h1>Bonjour {name} !</h1>
      <p>Tu as {age} ans.</p>
    </div>
  );
}
```

<Callout type="tip" title="Pourquoi la destructuration ?">
**Avantages :**
- Plus court et lisible
- Vous voyez immédiatement quelles props sont utilisées
- Auto-complétion plus facile dans votre éditeur

```jsx
// ❌ Répétitif
function UserCard(props) {
  return (
    <div>
      <h2>{props.name}</h2>
      <p>{props.email}</p>
      <p>{props.bio}</p>
    </div>
  );
}

// ✅ Clair et concis
function UserCard({ name, email, bio }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>{email}</p>
      <p>{bio}</p>
    </div>
  );
}
```
</Callout>

## Valeurs par défaut

Vous pouvez définir des **valeurs par défaut** pour les props.

```jsx
function Button({ text = 'Cliquez-moi', color = 'blue' }) {
  return (
    <button style={{ backgroundColor: color }}>
      {text}
    </button>
  );
}

// Utilisation
<Button />                           // Utilise les valeurs par défaut
<Button text="Envoyer" />            // text="Envoyer", color="blue"
<Button text="Annuler" color="red" /> // Les deux personnalisés
```

## Props en lecture seule

**Les props sont immuables** : vous ne pouvez **jamais** les modifier.

```jsx
function Greeting({ name }) {
  // ❌ ERREUR : Ne modifiez jamais les props !
  name = name.toUpperCase();

  return <h1>Bonjour {name}</h1>;
}
```

<Callout type="error" title="Les props sont read-only">
**Règle d'or :** Un composant ne doit **jamais** modifier ses propres props.

```jsx
// ❌ Interdit
function Counter({ count }) {
  count = count + 1; // ERREUR !
  return <p>{count}</p>;
}

// ✅ Créez une nouvelle variable si nécessaire
function Counter({ count }) {
  const nextCount = count + 1;
  return <p>Prochain : {nextCount}</p>;
}
```

**Pourquoi ?** Les props viennent du parent. Si vous les modifiez, vous créez des bugs imprévisibles.
</Callout>

<QuickCheck
  question="Peut-on modifier la valeur d'une prop à l'intérieur d'un composant ?"
  options={[
    "Oui, les props sont des variables normales",
    "Oui, mais seulement avec const",
    "Non, les props sont en lecture seule",
    "Oui, mais seulement pour les nombres"
  ]}
  correctAnswer={2}
  explanation="Les props sont en lecture seule (read-only). Un composant ne peut jamais modifier ses propres props car elles viennent du parent. Si vous avez besoin de calculer une nouvelle valeur, créez une nouvelle variable locale."
/>

## Passer des fonctions en props

Vous pouvez passer des **fonctions** comme props pour gérer les événements.

```jsx
function Button({ onClick, text }) {
  return <button onClick={onClick}>{text}</button>;
}

function App() {
  function handleClick() {
    alert('Bouton cliqué !');
  }

  return <Button onClick={handleClick} text="Cliquez-moi" />;
}
```

### Exemple : Champ de recherche

```jsx
function SearchBar({ onSearch }) {
  return (
    <input
      type="text"
      placeholder="Rechercher..."
      onChange={(e) => onSearch(e.target.value)}
    />
  );
}

function App() {
  function handleSearch(searchTerm) {
    console.log('Recherche :', searchTerm);
  }

  return <SearchBar onSearch={handleSearch} />;
}
```

<Callout type="tip" title="Convention de nommage">
**Props de fonction** : Préfixez avec `on` + Action

```jsx
<Button onClick={handleClick} />
<Form onSubmit={handleSubmit} />
<Input onChange={handleChange} />
<Modal onClose={handleClose} />
```

**Fonctions de gestion** : Préfixez avec `handle` + Action

```jsx
function handleClick() { /* ... */ }
function handleSubmit() { /* ... */ }
function handleChange() { /* ... */ }
```
</Callout>

## Spread des props

L'opérateur **spread** (`...`) permet de passer toutes les props d'un coup.

```jsx
function Input(props) {
  return <input {...props} />;
}

// Utilisation
<Input type="text" placeholder="Nom" required />
// Équivalent à :
// <input type="text" placeholder="Nom" required />
```

### Combiner spread et props spécifiques

```jsx
function Button({ variant = 'primary', ...rest }) {
  const className = `btn btn-${variant}`;

  return <button className={className} {...rest} />;
}

// Utilisation
<Button variant="danger" onClick={handleClick} disabled>
  Supprimer
</Button>
```

## Validation des props

### Avec TypeScript (recommandé)

TypeScript offre une vérification à la compilation et une excellente auto-complétion.

```tsx
interface UserCardProps {
  name: string;
  age: number;
  email?: string; // Optionnel
}

function UserCard({ name, age, email }: UserCardProps) {
  return (
    <div>
      <h2>{name}</h2>
      <p>{age} ans</p>
      {email && <p>{email}</p>}
    </div>
  );
}
```

Vous verrez TypeScript en détail dans la section dédiée.

### Avec PropTypes

PropTypes permet une validation à l'exécution (moins puissant que TypeScript).

```jsx
import PropTypes from 'prop-types';

function UserCard({ name, age, email }) {
  return (
    <div>
      <h2>{name}</h2>
      <p>{age} ans</p>
      <p>{email}</p>
    </div>
  );
}

UserCard.propTypes = {
  name: PropTypes.string.isRequired,
  age: PropTypes.number.isRequired,
  email: PropTypes.string
};
```

<Callout type="info" title="TypeScript vs PropTypes">
**TypeScript** : Vérification à la compilation
- Erreurs avant même de lancer l'app
- Auto-complétion parfaite
- Plus de sécurité

**PropTypes** : Vérification à l'exécution
- Warnings dans la console en développement
- Plus simple à ajouter à un projet existant
- Pas de vérification en production

**Recommandation :** Utilisez **TypeScript** pour de nouveaux projets.
</Callout>

## Exemple pratique : Carte de produit

```jsx
function ProductCard({ name, price, image, onAddToCart }) {
  return (
    <div className="product-card">
      <img src={image} alt={name} />
      <h3>{name}</h3>
      <p className="price">{price}€</p>
      <button onClick={() => onAddToCart({ name, price })}>
        Ajouter au panier
      </button>
    </div>
  );
}

// Utilisation
function App() {
  function handleAddToCart(product) {
    console.log('Ajouté:', product);
  }

  return (
    <ProductCard
      name="Laptop"
      price={999}
      image="laptop.jpg"
      onAddToCart={handleAddToCart}
    />
  );
}
```

## Bonnes pratiques

<Callout type="tip" title="Conseils pour bien utiliser les props">
**Destructurez les props**
```jsx
// ✅ Bon
function User({ name, email }) { /* ... */ }

// ❌ Moins bon
function User(props) {
  return <div>{props.name}, {props.email}</div>;
}
```

**Utilisez des valeurs par défaut**
```jsx
function Button({ text = 'OK', variant = 'primary' }) { /* ... */ }
```

**Ne modifiez jamais les props**
```jsx
// ❌ Interdit
function Component({ value }) {
  value = value + 1; // NON !
}
```

**Gardez les props simples**
```jsx
// ✅ Bon : Props simples et claires
<UserCard name="Alice" age={25} />

// ❌ Trop de props
<UserCard firstName="Alice" lastName="Dupont" age={25} email="..." phone="..." />

// ✅ Mieux : Groupez dans un objet
<UserCard user={{ name: 'Alice', age: 25, email: '...' }} />
```
</Callout>

## Résumé

<Callout type="info" title="À retenir">
**Props = Arguments de fonction**
- Permettent de passer des données du parent vers l'enfant
- Syntaxe : `<Component prop="value" />`
- Valeurs non-string avec accolades : `count={5}`, `enabled={true}`

**Recevoir les props**
- Objet props : `function Component(props) { props.name }`
- Destructuration (recommandé) : `function Component({ name, age })`
- Valeurs par défaut : `function Component({ name = 'Anonyme' })`

**Règles importantes**
- Les props sont **en lecture seule** (immuables)
- Ne jamais modifier les props reçues
- Les fonctions peuvent être passées en props (conventions : `onClick`, `handleClick`)

**Spread operator**
- Passer toutes les props : `<input {...props} />`
- Combiner avec props spécifiques : `{ variant, ...rest }`

**Validation**
- TypeScript (recommandé) : typage à la compilation
- PropTypes : validation à l'exécution

Les props sont essentielles pour créer des composants réutilisables et flexibles !
</Callout>

## Ressources

<Resources resources={[
  {
    title: "Passing Props to a Component",
    description: "Guide officiel React sur les props",
    url: "https://react.dev/learn/passing-props-to-a-component",
    type: "doc"
  },
  {
    title: "TypeScript with React",
    description: "Utiliser TypeScript pour typer les props",
    url: "https://react.dev/learn/typescript",
    type: "doc"
  },
  {
    title: "PropTypes",
    description: "Documentation de la bibliothèque PropTypes",
    url: "https://www.npmjs.com/package/prop-types",
    type: "doc"
  }
]} />
